# Database settings
DB_NAME=app
DB_USER=johndoe
DB_PASS=randompassw0rd
DATABASE_URL=postgres://${DB_USER}:${DB_PASS}@localhost:5432/${DB_NAME}

# Container settings
CONTAINER_NAME=rust-api-db
CACHE_CONTAINER_NAME=rust-api-cache

# API PORT
PORT=3001

PUBLIC_BASE_URL=http://localhost:${PORT}
VALKEY_URL=redis://localhost:6379

# App environment (development | production)
#APP_ENV=production
APP_ENV=development

# CORS (only used in production)
# Comma-separated list of allowed origins (no spaces recommended)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5172,https://app.example.com,https://admin.example.com

# SQIDS
SQIDS_MIN_LENGTH=10
SQIDS_ALPHABET=abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789

# AUTH settings
AUTH_ISSUER=https://takt.dev
AUTH_AUDIENCE=api.example.com
ACCESS_TOKEN_LEEWAY_SECONDS=60
DPOP_REQUIRED=true
DPOP_IAT_LEEWAY_SECONDS=60
DPOP_MAX_AGE_SECONDS=300
DPOP_REPLAY_TTL_SECONDS=300
DPOP_REQUIRED_ATH=true
DPOP_REQUIRE_NONCE=false

# ACCESS JWT (EdDSA)
# NOTE: put your Ed25519 public key PEM here. For multiline PEM in .env, use \n escapes.
# openssl genpkey -algorithm ed25519 -out ed25519-private.pem
# openssl pkey -in ed25519-private.pem -pubout -out ed25519-public.pem
# cat ed25519-public.pem
ACCESS_JWT_PUBLIC_KEY_PEM=""
